package com.comparatorvcomparable;

public class MyString extends String{

	// We cannot extend a final class
	// FINAL --> you cannot create a subclass
	// final methods: you cannot override them
	// final variables: are constant - can't be modified 
	
	// what's the difference between final, finally, and finalize()
	// finally we used with exceptions
	// finalize() we use with Garbage Collection and memory management.
	
}
