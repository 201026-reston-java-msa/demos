export interface Cat {
    id: number;
    name: string;
}

// An interface is a specification that identifies a related
//set of properties and methods to be implemented by a class

// We use interfaces to build custom TYPES in TypeScript